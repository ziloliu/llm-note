---
title: "串口通信协议基础"
category: "STM32/USART"
tags: [串口, USART, 异步通信, 数据帧, 波特率]
abstract: "串口通信协议的基础概念，包括数据帧格式、波特率、校验等核心知识点"
source: "原创"
update_time: 2026-05-31
status: 完成
type: 原理
---

## 一句话定义

串口通信是一种串行、异步、全双工的点对点通信协议，通过TX/RX两根数据线实现设备间数据传输。

## 核心内容

### 基本定位

| 项目 | 内容 |
|------|------|
| 传输方式 | **串行**（逐位传输） |
| 时钟方式 | **异步**（无时钟信号线） |
| 数据方向 | **全双工**（独立 TX/RX，同时收发） |
| 连接方式 | **点对点**（不支持总线挂多设备） |

> 串口通信是串行通信的一种，但不等于串行通信。串行通信是一大类，串口是其中一种具有特定协议标准的方式。

### 硬件连接

**引脚定义：**

| 引脚 | 全称 | 功能 |
|------|------|------|
| **TX** | Transmit | 发送数据端 |
| **RX** | Receive | 接收数据端 |

**连线规则：**
```
设备A              设备B
  TX ──────────→ RX     （A 的发送 连 B 的接收）
  RX ←────────── TX     （A 的接收 连 B 的发送）
```

- 必须**交叉连接**：TX→RX，RX→TX
- 仅需**两根数据线**，无时钟线
- **点对点**连接，不支持总线拓扑

### 数据帧格式

串口以**数据帧**为单位发送数据，一帧包含以下部分：

```
空闲位  起始位  数据位(低位→高位)  [校验位]  停止位  空闲位
  1      0     D0 D1 D2 ... D7     P        1       1
(高)    (低)   (从LSB到MSB)      (可选)     (高)    (高)
```

**各字段说明：**

| 字段 | 电平 | 位数 | 说明 |
|------|------|------|------|
| **空闲位** | 高电平（1） | 持续不定 | 无数据传输时线路保持高电平 |
| **起始位** | 低电平（0） | **1 位**（固定） | 检测到高→低跳变，标志数据传输开始 |
| **数据位** | 高/低 | **5~8 位**（一般 8 位） | 有效数据，**先发 LSB，后发 MSB** |
| **校验位** | 高/低 | **0 或 1 位**（可选） | 奇偶校验，用于简单错误检测 |
| **停止位** | 高电平（1） | **0.5 / 1 / 1.5 / 2 位**（可约定） | 标志一帧数据传输结束 |

### 波特率（Baud Rate）

**为什么需要波特率：**
- 串口通信**无时钟信号线**，接收方不知道每个 bit 持续多长时间
- 双方必须**预先约定相同的传输速率**，才能正确解析每一位数据
- 波特率就是双方约定的**每秒传输码元数**

**波特率与比特率关系：**

| 概念 | 定义 | 关系 |
|------|------|------|
| **波特率（Baud Rate）** | 每秒传输的**码元**数 | 物理层信号单元 |
| **比特率（Bit Rate）** | 每秒传输的**比特**数 | 数字层信息单位 |

在串口通信中（二进制信号）：一个码元 = 1 bit，波特率 = 比特率

**常见波特率：**

| 波特率（bps） | 说明 |
|:------------:|------|
| 9600 | **最常用** |
| 115200 | **高速常用（调试场景）** |

### 奇偶校验

**原理：** 统计**数据位 + 校验位**中所有 "1" 的个数

| 校验方式 | 要求 |
|----------|------|
| **奇校验** | "1" 的总个数为**奇数** |
| **偶校验** | "1" 的总个数为**偶数** |

**校验能力：**
- 优点：实现简单，底层电路成本低
- 缺点：仅能检测**奇数位**错误，偶数位错误无法检测
- 更强方案：CRC（循环冗余校验）

### 空闲帧检测

**问题：** 发送变长数据（如字符串）时，接收方如何判断数据已全部发送完毕？

**解决方案：空闲帧**

| 项目 | 说明 |
|------|------|
| 定义 | 持续一个完整数据帧长度的**全高电平**（全为空闲位） |
| 作用 | 标志当前通信彻底空闲，变长数据发送结束 |
| 检测方式 | 接收方检测线路持续高电平超过一帧长度即判定为空闲帧 |

## 注意事项 & 踩坑

- 串口 ≠ 串行：串口是串行通信的一种具体协议标准
- 异步传输：无时钟线，靠约定波特率同步
- 传输顺序：LSB 先发，MSB 后发
- 空闲状态：高电平
- 起始位：低电平（检测高→低跳变）
- 停止位：高电平
- 收发双方波特率必须一致

## 相关笔记

- [[UART与USART概念辨析]]
- [[USART功能框图详解]]
- [[USART波特率配置]]

## 参考来源

- STM32F103参考手册
- 串口通信协议标准文档
